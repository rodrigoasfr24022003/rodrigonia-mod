/*
const rodrigoniaitems = require("rodrigonia/items")
const BUFFER_CORES = ["Core: Crane"];
let missiler = extend(ItemTurret, "missiler", {
    updateTile(){
        this.super$UpdateTile();
        if (this.team.cores().contains(coreBuild => BUFFER_CORES.includes(coreBuild.block.name))){
            this.reload = 15
        } else {
            this.reload = 30
        };
    },
});
*/